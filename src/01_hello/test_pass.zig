const std = @import("std");
const expect = std.testing.expect;

test "Can I name this whatever I want?" {
    // So not a very helpful test, but it shows how to use this assert equivalent.
    try expect(true);
}
